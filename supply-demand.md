# 수급 (외국인/기관 순매수 기반 종목 선정)

> 설계 전문은 memory `project_supply_demand`(4관점 제로베이스 통합안) + `reference_lpmm_supply_postmortem`(과거 LP_MM_TRADING 실패 부검). 이 문서는 구현 스펙 요약.

## 핵심 원칙 (부검 교훈 — 위반 금지)

- **지표 정의는 1벌** — `backend/services/flow_metrics.py`가 유일한 정본. 화면/상세/백테스트 전부 이 모듈의 소비자. 프론트는 포맷팅만 (재계산 금지).
- **합성 점수 없음** — 모든 컬럼이 HTS로 검산 가능한 raw 산수. 정렬 키 = 외인 20D bp 단일.
- **파라미터는 코드 상수** (윈도우 5/20/60, 진입 임계, 필터 하한) — 사용자 노브는 필터 프리셋·방향·검색만.
- "이상한 종목이 상위에" 문제가 생기면 보정 레이어 추가 금지 — 지표 정의 교체 + 백테스트 비교로 대응.

## 지표 정의 (flow-v1)

```
수급강도(N일) = Σ순매수금액(N일) ÷ 유통시총 × 10,000  [bp]
유통시총 = 유통비율(as-of floating_shares/total_shares) × 최신 market_cap
```

- N ∈ {5, 20, 60}, 주체 = FOREIGN / INSTITUTION.
- **유통비율 × 최신 시총**인 이유: 유통주식수 절대값은 base_date에 묶여 증자·분할 시 왜곡 (실측: 027360 증자 → 절대값 분모가 bp 2배 왜곡). 비율은 완만, 시총은 매일 갱신.
- 분모에 adj_close 금지 (분할 시 과거 소급 재작성 — point-in-time 위반). 수익률·차트에만 사용.
- **PENSION은 INSTITUTION의 부분집합 — 합산 금지** (DB 검증: 외+기+개 일합 ≈ 0). 개인은 −(외+기) 근사라 독립 팩터로 쓰지 않음.

## 시그널 규약

- **D일 수급은 D일 장 마감 후 확정 → D 신호는 D+1 시가부터 실행 가능.** 백테스트 진입가는 반드시 D+1 open (look-ahead 금지).
- **진입권/매도권 뱃지** (트레이더 설계 임계, 상수): `|20D| ≥ 15bp AND (연속 ≥ 3D or |5D| ≥ 0.3×ADV20)` — 증자·블록 등 단일 이벤트성 스파이크를 지속성 조건으로 배제. 필터가 아니라 뱃지 (행 숨김 없음).
- **단기반등 뱃지 + 장기 정합 필터** (20일 창의 장기 맥락 결핍 보완): `20D 순매수 > 0 AND 120D(반년) 순매수 < 0` = "장기 분산+단기 반등"(리노공업 사례: 20D +14% / 120D −5% / 1년 −2260억). **정렬은 20D 유지(반응성=신호), 120D는 맥락**(정보라 검증 부담 없음 — 앞서 '기간 확대 반려'와 일관). 20D 셀에 120D 병기. "장기 정합만" 체크박스 = `long_up`(120D>0)만.
- 매도 후보 = 같은 지표의 오름차순 (별도 공식 없음). 동시 뱃지 = 외인∧기관 20D 동시 순매수. NEW = 전일 상위 50에 없던 신규 진입.

## API (FastAPI, `routers/flow.py`)

- `GET /api/flow/meta` — as_of·is_partial·파라미터·데이터버전·규약
- `GET /api/flow/ranking?preset=default|large|all` — 랭킹 (f_20d_bp 내림차순 정렬됨)
- `GET /api/flow/stocks/{code}?days=365` — 일별 외인/기관 순매수(억) + 누적 + 수정종가 (최대 3년)

캐시 = 데이터 버전 기반 (시간 TTL 아님): 프로브(60s) = (max(time), 그 날짜 row수) → 바뀌면 자동 재계산. 크론/수동 무효화 없음. `is_complete` 게이트 = 최신일 종목 커버리지 < 직전 5일 중앙값의 90% → as_of 전일 강등.

## 데이터 (Finance_Data PG, read-only)

- `investor_trading`: time / stock_code / investor_type(FOREIGN·INSTITUTION·PENSION·RETAIL) / net_buy_value(원). 2022-01~, 장 마감 후 크롤러 갱신. **주식 전용 (ETF 없음)**.
- 보조: `ohlcv_daily`(close_price raw·adj_close·trading_value), `market_cap_daily`, `floating_shares`(base_date 이력), `stocks`, `stock_sectors`.
- ⚠️ **알려진 이슈**: `floating_shares`가 2026-05-21부터 NULL 적재 (Finance_Data 크롤러) — as-of 조인이 "가장 최근의 유효값"(현재 02-19)을 사용, 화면에 신선도 경고 표시. Finance_Data 측 수정 필요.

## 화면 (`frontend/src/pages/stock-flow.tsx`, `/supply-demand`)

- **표기**: bp 대신 **"매집%"**(bp/100) — "유통물량의 몇 %를 순매수했나"가 직관적. 예: +14.4% = 20일간 유통주식의 14.4%를 외인이 순매수.
- **요약 스트립**: 외인 20D 순매수/순매도 종목수 · 진입권/매도권 수 · 동시매수 수.
- **컬럼 8개**: 종목(이름·코드·섹터·유통시총) / 연속(부호) / 외인 5D(억) / **외인 20D 매집%(정렬)** / 기관 20D% / 흡수율(5D 순매수÷거래대금) / 20D 수익률(수정주가) / 어제(외/기). 매수·매도 토글, 프리셋 3종, 검색, 기준일·partial 배지, 데이터 품질 경고. **"장기 정합만" 필터 기본 ON**(120D>0인 종목만 — 장투 정합; 해제 시 '장기 분산+단기 반등'도 노출).
- **상세 차트** (`components/flow/flow-detail.tsx`, 행 클릭) — alphasquare "투자자별 매매동향" 참고, 2×2 격자 4분할: ① 주가(수정종가 캔들 + 이평 50·100·200 + VWMA200 + 외인 평단 추정선) ② 순매수 모멘텀(외인/기관 토글, 막대 + 5·20일선 = 최근 매수 강도) ③ 누적순매수(외인/기관 토글, 누적선 + 20·50·100·200일 이평 + ▲골든/▼데드크로스 마커[20×100] = OBV+시그널선, 차트↔테이블) ④ 순매수 일별(외/기/개, 차트↔테이블). **이평 배치 원칙**: 추세·크로스는 추세를 갖는 누적(③)에서만 의미 → 일별(②)엔 강도용 단기 MA만. **캔들색**=트레이딩뷰 기본(양봉 #089981/음봉 #f23645, 캔들 전용 상수). **전 차트 호버 툴팁**: 크로스헤어 날짜의 값을 색 스와치+정렬 카드로(①시고저종·등락 ②순매수·5·20일 ③누적·4개 이평+크로스 신호 ④외/기/개). 1년/3년 토글, **4개 차트 시간축 동기화**(logical range subscribe — autoSize:true 필수, grid-cols-2에서 우측 열 생성 폭 0이면 sync 깨짐). Lightweight Charts.

## 로드맵

- **v1 (완료)**: 랭킹 화면 + API.
- **v1.5 (완료)**: bp→매집% 표기, 요약 스트립, 종목 상세 차트(가격+외인 평단선 + 일별 순매수 막대+누적선 1~3년).
- **v1.6 (완료)**:
  - **관심종목 고정 섹션** — localStorage(`flow.watchlist`) 별표. 상단 고정, 흐름 약화(외인 순매도 전환/20D 음수) 판정.
  - **분배 패턴 뱃지** — 외인 5D 순매도(≤−10bp) + 주가 방어(5D −2% 이내) + 개인 순매수. ranking에 RETAIL 5D·ret_5d 추가(정본 SQL). "조용히 무너지기 전" 신호 — 매수 부호반전으로 안 잡힘.
  - **상세 차트 4분할** — 주가(캔들+이평 50·100·200+VWMA200+외인평단)/외인 순매수 모멘텀/누적순매수/순매수 일별. 4개 시간축 동기화. (초기 만기·MSCI 세로 마커는 도입했다가 사용자 요청으로 제거 — 노이즈.)
- **v1.7 (완료)** — 상세 차트 마감:
  - **차트 동기화 정착** — 4개 2×2 격자에서 줌·팬 동기화. 원인: grid-cols-2 우측 열 차트 생성 시 폭 0 → `setVisibleLogicalRange` 무시. `createChart(autoSize:true)`로 내부 ResizeObserver가 폭 확정 후 정상화(수동 width/useResize·chartRef 제거). 순서 ③누적↔④일별.
  - **② 모멘텀 외인/기관 토글** + 5·20일선(=최근 매수 강도). ③ **누적에 크로스 이평 도입** — 20·50·100·200일 + ▲골든/▼데드크로스(20×100). *일별엔 강도용 단기 MA, 추세·크로스는 누적에서* 원칙 정립.
  - **전 차트 호버 툴팁** — 공통 `attachTooltip` 헬퍼(색 스와치+라벨/값 정렬 카드). ③은 크로스일에 골든/데드 신호 강조.
  - **캔들색 트레이딩뷰 통일**(#089981/#f23645, 캔들 전용 상수). **외인평단선** 회색·얇게·텍스트 태그 제거(축 값만).
  - **3년 이평선 버그 수정** — 러닝-합 SMA가 null(거래정지)일에 NaN 오염 → 이후 전체 소실. 실거래일(priced rows)만으로 계산.
  - **"장기 정합만" 필터 기본 ON**.
- **v1.8 (완료, 2026-07-05)** — 판정 컬럼 + 태그 범례 ("어느 종목이 제일 괜찮은지 한눈에", PR-1 측정 근거):
  - **PR-1 측정** (flow-tag-backtest.md §PR-1): h=120 지평 추가(신호 단조 강화, IC t7.75) + 조건부 IC — **🔴 f120>0 서브셋 안에선 외인 20D 정렬 IC 무의(t<0.5)** = "장기 정합만" 필터 상태의 세부 순위는 판별력 없음, 멤버십(태그)만 유효. cons120(순매수일 비율)은 무가치 — 미도입. → 판정은 정렬 세분화가 아니라 **태그 기반**으로 확정.
  - **판정 정본 공용화** — `backend/services/flow_verdict.py`: `applicable_patterns(row)`(매수 아키타입 정석>진입권>추세순항>동시 배타 1개 + 경고 중복) + `verdict(row, edges)`(|t|≥2 통과분 중 |edge| 최대 대표). `flow_ai._assess`가 이걸 호출(공식 1벌). `/api/flow/ranking` row에 `verdict`, 응답에 `edges`/`edges_as_of`.
  - **판정 컬럼**(종목 다음): 대표 패턴명 + 측정 edge(`+3.5%` 초록/빨강), 정렬 가능, 툴팁에 백테스트 근거. **합성점수 아님 — 측정값 조회, 튜닝 파라미터 0** (계수는 flow_backtest.json 자동 갱신).
  - **히트 컬러** — 외인 20D% 셀에 표시 대상 내 분위 배경(상위/하위 10%·25%).
  - **태그 범례 패널** — 토글 버튼(② 토글 동일 디자인): 태그별 일상어 한 줄 + 측정 edge·t 동적 표시(|t|<2는 "유의성 미달" 표기) + 초과수익 정의·검증 기준일 각주.
- **v1.9 (완료, 2026-07-05)** — 사용자 피드백 8건 반영 + 장기동시 채택 (flow-tag-backtest.md §PR-4a):
  - **장기동시 태그 신설** (백테스트 검증 후): 외인·기관 20D·120D 4중 순매수 = h60 +3.63%(t6.70) — 동시의 3배·정석 동급·t 최고. SQL `i_120d` 추가(f_120d 미러, 스캔량 증가 0), `long_both`·`i_120d_bp` 필드, 배타 체인 **장기동시 > 정석 > 진입권 > 추세순항 > 동시**, 뱃지 승격(장기동시면 하위 매수 뱃지 대신 단독).
  - **컬럼 개편**: # / ★(독립) / 종목(이름·코드·섹터) / 유통시총(독립·정렬) / 태그(독립 열 — 종목 셀 뱃지 이동, 판정과 중복 해소) / 판정 / 연속 / 외5D / 외20D(+120D) / 기관20D(+120D 병기 신설) / 흡수율 / 20D수익률 / 어제.
  - **태그 색 시맨틱 통일**: 강세=초록(장기동시 최강조), 약세=빨강(동반순매도·분배), 경고=주황(단기반등), 정보=파랑(NEW) — 백테스트 방향 기준.
  - **헤더 툴팁**: native title → CSS `Tip` 컴포넌트(제목+설명, 유통시총~어제 전 헤더 + 판정 셀).
  - **매도 뷰 "장기매집 제외" 필터 기본 ON** (두산로보틱스 교정): f120>0 종목은 매도후보에서 숨김 — 백테스트상 '매집주 눌림'(+2.3% 강세)이라 매도후보 부적격. 동반순매도의 약세 edge는 f120≤0 조건부.
  - **"정렬 ≠ 추천" 명시** (외20D 툴팁·범례 상단): 정렬은 수급 강도 순서일 뿐, 상위권 내 세부 순위는 예측력 없음(PR-1 조건부 IC≈0) — 판단은 판정·태그로.
  - AI 요약 facts에 기관 120D 매집률 주입(외인과 대칭).
- **v1.10 (완료, 2026-07-05)** — 깔때기 완성 + 매도 타이밍 + UX 마감 (백테스트 상세는 flow-tag-backtest.md §PR-5·PR-6):
  - **태그·판정 열 통합**: 판정 열 제거, 색 뱃지 안에 검증 edge % 병기(`[장기동시 +3.6%]`). 복수 패턴 전부 표시(강세·약세 상충 노출), 대표(|edge| 최대) 기준 정렬. **|t|<2 패턴은 뱃지 동적 숨김**(edges 기준 — '하락추세 매집' 미표시, 검증 통과 시 자동 재등장). 패턴 멤버십은 `/ranking row.patterns`(백엔드 `flow_verdict.applicable_patterns` 정본 — 프론트 미러 금지, 공식 1벌).
  - **요약 칩 필터** (클릭 = 해당 종목만 표시, 카운트=행수, 라디오식, 검색 AND, 방향 전환 시 해제): 매수 계열(edge 순) **핵심 후보 → 강세 후보** → 진입권 → 추세순항 → 동시매수 → 매도 계열(매도권 → 분배 의심). 카운트 색은 핵심 후보만 초록.
    - **핵심 후보** (PR-5 채택) = 장기동시 ∧ 진입권 ∧ 경고 없음 — ~31종목/일, h60 +5.28%(t5.3)·h120 +12.6%. 흡수율≥10% 게이트는 edge 역효과로 기각.
    - **강세 후보** = 검증 강세 태그 1개 이상 ∧ 경고 태그(분배·단기반등·동반순매도) 없음 (~256종목).
    - 깔때기: **핵심 후보(~31) → 강세 후보(256) → 뱃지 edge%·경고 동반으로 비교 → 상세차트·AI요약으로 확정.** 풀 안 순위는 예측력 없음(측정) — 순위표 대신 후보 풀.
  - **보유·매도 가이드 (PR-6)**: 범례 패널 상주 — 매수 태그 종목 **~120거래일(약 반년) 보유 최적**(핵심 후보는 61~120일 구간 +6.5%가 첫 60일 +5.3%보다 큼) / 태그 며칠 꺼짐은 소음(부호 게이트 토글) / **경고 발생 = 손절 아닌 로테이션 신호**(잔여 초과 +3.8→+0.7%, 음수 아님 — 대체 후보 있을 때만 교체) / 매집주 눌림은 조기청산 금지. 모든 %는 유니버스 평균 대비 초과수익.
  - **관심종목 보유 단계**: watchlist v2(code→등록일, 구버전 자동 마이그레이션). `D+n`(주말 제외 거래일 근사), **120거래일 도달 시 주황 ⚠**(알파 소진 — 교체 검토), 경고 태그 발생 시 뱃지+주황 보더. 헤더에 보유 규칙 요약 한 줄.
  - **UX 마감**: 별표·유통시총·태그 독립 열 / 기관 20D 히트 음영(기관 값 기준 별도 분위, 외인과 동일 규칙: 표시 종목 내 상·하위 10%/25%) / 5D·어제 열 외/기 병기 / 태그·판정 우측 정렬 / 정렬 삼각형은 활성 열만(종목차익 방식) / 헤더·칩 전부 Tip 컴포넌트(조건·근거·주의 구조화, whitespace-normal로 넘침 수정) / **색상 절제 = 대차 대여가능확인 규칙**(핵심 신호 셀만 상승/하락 색, 보조 수치 회색) / 선택 종목 행 최상단 고정(+원 순위 보존)·차트로 스크롤 / 분배 태그에 시간창(5일) 명시(20·120일 매집 태그와 공존 가능 — 시간창 상이) / 툴팁에 '거래일' 단위 명시(미래산업 +49.8% = 20거래일 전 대비, 달력 20일 아님) / 범례 각주에 갱신 주기(2년 룩백, 30일 경과 시 서버 시작 자동 재계산 ≈ 월 1회).
  - **데이터 이슈 해결**: stock_sectors 7/4 NULL 덮임 → 7/5 Finance_Data 측 수정 완료(Naver GICS식 명칭). stock_sectors는 매핑 테이블이라 주말 갱신 자체는 무해.
- **v2 (백테스트 입증 조건부)**: z-score·EWMA·섹터중립·초과수급. 배당락 마커. (아래 백테스트 계획 참조)
  - **[flow-tag-backtest.md](flow-tag-backtest.md)** — 태그 검증 게이트 1차 실행(2026-07-03). 정렬축(외인 20D) Rank IC 유의(+). 채택=진입권·정석(동시+진입권)·동시·추세순항. **반려=저점재매집(강한 언더퍼폼 — "떨어지는 칼" 실증)**. 스크립트 `backend/scripts/flow_tag_backtest.py`.
  - **AI 수급 요약 버튼**: `backend/services/flow_ai.py` + `/api/flow/ai-summary/{code}`. 외부망 라이브 검증 완료(claude-sonnet-5). 사실(숫자)은 코드가 계산·LLM은 종합 판단만. **핵심: 백테스트 판정(`_assess`: 패턴별 검증 초과수익 ±%)을 사실로 주입** → 단순 나열이 아니라 **강세/약세 요인 분리 + 상충 지적**(예: 정석 +3.49% vs 하락추세매집 −5.19%). LLM 입력은 한국어 라벨 렌더(영문 키 누수 차단). 가드레일: 매매 지시어 금지(프롬프트+후처리 필터)·면책 문구·데이터버전 캐시. `ANTHROPIC_API_KEY` 없으면 graceful degrade(내부망 비활성). 키는 루트 `.env` 직접 폴백 조회.
- **v2 (백테스트 입증 조건부)**: z-score·EWMA·섹터중립·초과수급. 백테스트 = LP_MM 골격 이식 + T+1 시가 + Rank IC(h=5/20/60/**120**) + 모멘텀 통제 + holdout 1회 개봉. 장기보유 니즈는 창 확대가 아니라 h=120 측정으로 판단 (결정 로그 참조).
