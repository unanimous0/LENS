# 수급 태그 백테스트 (검증 게이트)

> 부검 원칙 "검증 없이 라벨 붙이지 말라"의 실행. 태그 후보가 실제로 미래 수익률을
> 예측하는지 look-ahead 없이 측정하고, **통과한 태그만** `flow_metrics`에 싣는다.
> 스크립트: `backend/scripts/flow_tag_backtest.py` (Finance_Data read-only).

## 방법 (look-ahead 차단)

- 신호는 **D일 장 마감까지** 데이터로만 계산 (trailing window).
- **진입 = D+1 시가(adj_open), 청산 = (D+1+h) 시가.** `fwd_h = 청산/진입 − 1`.
- **초과수익 = 그날 유니버스 평균 대비** (시장 베타 제거).
- **Rank IC**: 날짜별 횡단면 스피어만(신호 vs fwd) → 날짜 평균 + t값.
- **태그**: 날짜별 (태그군 평균 − 유니버스 평균) 스프레드 → 날짜를 관측단위로 t검정
  (횡단면 상관 부풀림 방지). 주간(5거래일) 리밸런스.
- 유니버스: 활성 KOSPI/KOSDAQ · 일평균 거래대금 ≥ 10억 · 시총 ≥ 500억.
- 정규화 프록시: 분모를 유통시총 대신 **market_cap**(floating_shares NULL 이슈 회피). Rank IC는 단조변환에 견고.

## 실행 규모 (2026-07-03 기준)

- 유니버스 **1,799종목**, 기간 **2023-04-22 ~ 2026-07-03**, 패널 수급 384만 rows.

## 결과 ① — 정렬축은 유효 (외인 20D/시총)

| 보유 h | Rank IC | t값 |
|---|---|---|
| 5일 | +0.021 | 3.34 |
| 20일 | +0.032 | 5.32 |
| 60일 | +0.040 | 6.00 |

→ **양(+)·유의(t>3)·호라이즌 증가에 단조.** 외인 20D 매집 정렬은 실제 알파가 있다. 랭킹 근간이 통계적으로 정당화됨.

## 결과 ② — 태그 초과수익 (유니버스 평균 대비, T+1 시가)

| 태그 | h20 초과% (t) | h60 초과% (t) | 판정 |
|---|---|---|---|
| **진입권** | +1.29 (4.79) | +2.84 (6.22) | ✅ 채택 (최강, 단조↑) |
| **정석(동시+진입권)** | +1.42 (3.91) | +3.49 (5.16) | ✅ 채택 (최대 크기) |
| **추세순항**(동시+20D수익률+) | +0.82 (3.16) | +1.52 (3.42) | ✅ 채택 |
| **동시**(외+기 20D 동반) | +0.67 (3.58) | +0.89 (3.01) | ✅ 채택 (완만) |
| 동시함정(동시지만 기관 5D−) | +0.46 (1.77) | +0.81 (1.51) | ⏸ 보류 (약함·유의 경계) |
| 분배 | −0.84 (−2.31) | −1.61 (−2.65) | ⚠️ 경고 유효 (음의 초과 = 회피 신호) |
| 단기반등(20D+ & 120D−) | −0.19 (−0.90) | −1.08 (−3.24) | ⚠️ 경고 유효 (장기 h에서 언더퍼폼) |
| **저점재매집**(20D+·120D+·<200MA·흡수↑) | **−2.51 (−4.46)** | **−5.19 (−3.68)** | ❌ **반려 (강하게 언더퍼폼)** |

## 핵심 발견 & 판정

1. **정렬축(외인 20D)은 알파가 있다** — 근간 유지.
2. **채택할 매수 태그: 진입권 · 정석(동시+진입권) · 동시 · 추세순항.** 전부 양의 초과수익 + 유의. "지속성(진입권)"과 "기관 동반(동시)"이 실제로 미래 수익을 높인다 — 트레이더 설계 임계가 데이터로 확인됨.
3. **경고 태그로 유효: 분배 · 단기반등** — 음의 초과수익이라 "회피" 신호로 정확. 이미 화면에 있는 용도 유지.
4. **❌ "저점재매집" 반려 (중요·반직관):** 하락추세(200일선 아래)에서의 외인 매집은 **평균적으로 크게 언더퍼폼**(h60 −5.19%, t−3.68). = "떨어지는 칼"의 실증. **앞서 일동제약을 '저점 재매집'으로 조심스레 긍정 평가한 서사는 데이터로 반박됨** — 개별 사례가 아니라 패턴 평균으론 지는 자리. **이것이 백테스트 게이트가 존재하는 이유**: 그럴듯하지만 틀린 태그를 출하 전에 걸러냈다.
5. 동시함정 보류 — 동시(+0.67)보다 약해(+0.46) "함정" 방향성은 맞지만 단독 유의성 부족.

## 추가 검증 — 매도·이탈 패턴 (2026-07-03, AI 요약 커버리지용)

AI 요약이 "외인·기관 최근 순매도" 종목(예: 현대건설)에 붙일 근거가 없어 추가 백테스트:

| 패턴 | 조건 | h60 초과% (t) | 판정 |
|---|---|---|---|
| **매집주 눌림** | 장기매집(120D+) & 최근 외인 이탈(20D−) | **+2.01 (4.81)** | ✅ 강세 (반직관) |
| **동반순매도** | 외·기 20D 동반 순매도 & 장기매집 없음(120D≤0) | **−2.35 (−6.74)** | ✅ 약세 (순수 이탈) |

**🔴 또 한 번의 반직관 발견:** "장기 매집 후 최근 이탈 = 경고"라는 직관이 반박됨. **장기 매집주가 단기 이탈(눌림)하면 오히려 평균 +2.01% 초과수익**(눌림목 성격). 반대로 **장기 매집 맥락이 없는 순수 동반 이탈만 −2.35% 언더퍼폼.** `f120` 부호가 신호를 뒤집는다 → 두 패턴은 배타적. (현대건설은 f120 +820bp라 '매집주 눌림'=강세 쪽.) `_assess`(flow_ai)에 둘 다 반영.

## 1단계 구현 — 완료 (2026-07-03)

`flow_metrics._row_to_metrics`에 검증 통과분만 상수 임계로 반영:
- ✅ **신규 매수 태그 `trend_ride`(추세순항)** = both_20d & 20D 주가 상승(adj_now>adj_prev). 화면에서 **동시 뱃지를 추세순항으로 승격**(trend_ride면 추세순항, 아니면 동시 — 중복 방지) + 요약 스트립 "추세순항 N" 카운터.
- 진입권·동시·분배·단기반등은 이미 필드·뱃지 존재 → 백테스트가 알파 확인만.
- ❌ **`저점재매집`은 매수 태그로 만들지 않음** (반려). 경고로 넣으려면 랭킹 SQL에 200일선 필요(현 px CTE 130일 → 확장 비용) — 보류.
- 가드레일 준수: 행 숨김 없는 뱃지 + 툴팁 근거, 정렬키 외인 20D 단일 유지, 동시표시 절제(동시↔추세순항 승격으로 개수 불변).

## 주기 갱신 자동화 (2026-07-03)

검증 결과를 코드 상수 대신 **JSON 파일로 저장 → 주기 재실행 → flow_ai가 소비**하도록 자동화:

- **저장:** `flow_tag_backtest.py --save data/flow_backtest.json` — `_canonical_masks`(런타임 `_assess`와 **정확히 동일한** 조건)의 h60 초과수익·t·방향을 측정해 JSON 기록. `{generated_at, universe_n, patterns:{name:{h60_excess_pct,t,direction}}}`.
- **소비:** `flow_ai._load_edges()` — JSON(mtime 캐시) 우선, 없으면 하드코딩 기본값 폴백. `_assess`는 **|t|≥2(유의) 패턴만** 주입(노이즈 방지). 요약에 `검증 기준일` 표기.
- **갱신:** `main.py` startup 핸들러 `_refresh_flow_backtest_if_stale` — JSON이 없거나 30일 초과면 **백그라운드 subprocess**로 재실행(무거운 pandas라 이벤트 루프 격리, lock으로 중복 방지). 실패해도 기본값 degrade. Finance_Data read-only.
- `data/`는 gitignore → JSON은 커밋 안 됨(각 배포가 자체 생성). 첫 배포는 기본값으로 시작 후 startup이 채움.

**부수 교정:** 조건 일치 과정에서 `하락추세 매집`(런타임: f20>0·f120>0·ret20<0, 200MA 없음)의 실제 edge가 **+0.65%(t1.62, 유의성 미달)**로 측정됨 — 앞서 붙였던 −5.19%는 더 엄격한 저점재매집(200MA<가·흡수↑) 수치의 오귀속이었음. |t|≥2 필터로 자동 제외되어 요약에서 사라짐.

## 룩백 기간 비교 & 2년으로 변경 (2026-07-04)

`--years`로 반년/1년/2년/3년 룩백을 각각 측정:

| 룩백 | Rank IC(h60) | 정석 | 진입권 | 동시 | 특징 |
|---|---|---|---|---|---|
| 0.5년 | −0.009 (t−0.41) ❌ | −1.88 | +0.40 | −1.79(t−3.1) | 표본부족·신호 뒤집힘·장기패턴 측정불가 |
| 1년 | +0.037 (t3.24) | +2.89 | +1.78 | +0.65 | 겨우 작동, 과적합(매집후이탈 +4.6%) |
| **2년** | **+0.050 (t7.15) ✅** | +3.53 | +2.95 | +1.20 | **최강·최선 (스위트스팟)** |
| 3년 | +0.043 (t6.83) | +3.45 | +2.88 | +0.91 | 2년과 수렴·robust, 약간 완만 |

**결정: 룩백 2년 채택** (`YEARS=2.0`). Rank IC·패턴 edge가 가장 날카롭고, 2·3년이 수렴해 신뢰 가능. 1년 이하는 결론이 뒤집힐 만큼 위험(0.5년은 정렬축 자체가 무효). 반응성 원하면 2년, 안정성 원하면 3년 — 2년이 균형점. 부수: 최근 6개월(0.5년)은 수급 정렬축이 사실상 안 먹힘(노이즈 가능성, 주기 갱신본으로 관찰).

## PR-1 — 중장기(120D) 지속성 축 측정 (2026-07-05)

배경: "5D/20D는 유입 감지엔 좋지만 장기 지속성 판단이 어렵다"는 문제의식. 창을 늘리는 대신 **기존 f120 하나를 게이트/태그로 승격할 근거가 있는지 측정** (h=120 지평 + 조건부 IC + 순매수일 비율 cons120). 스크립트 확장: `HORIZONS=[5,20,60,120]`, `_rank_ic` 헬퍼(서브셋 마스크), ③ 조건부 IC / ④ cons120 섹션, `T_지속매집` 태그 후보.

**① h=120에서 신호 유지·강화** — 정렬축 IC 0.0491(t7.75, 날짜 69). 양의 태그(h120: 진입권 +5.92%, 정석 +7.49%, 지속매집 +4.93%)와 경고 태그(저점재매집 −11.63, 동반순매도 −6.14) 모두 h60 대비 크기 확대·부호 유지. **중장기 보유일수록 신호가 단조 강화** — 장기보유 관점 정당화.

**③ 🔴 반직관: "장기 정합(f120>0)" 게이트는 랭킹을 개선하지 않는다 — 오히려 알파를 제거:**

| h | (a) 전체 IC (t) | (b) f120>0 IC (t) | (c) f120≤0 IC (t) |
|---|---|---|---|
| 20 | 0.0388 (5.81) | 0.0009 (0.10) | 0.0464 (4.81) |
| 60 | 0.0495 (7.22) | 0.0042 (0.46) | 0.0512 (4.86) |
| 120 | 0.0491 (7.75) | −0.0051 (−0.46) | 0.0403 (3.88) |

f120>0 서브셋 **안에서는** 외인 20D 정렬이 전 지평 무의(t<0.5). 정렬축 알파는 대부분 f120≤0 쪽(동반순매도 회피 등 매도 사이드 판별 포함)에서 발생. → **"장기 정합만" 필터를 켠 상태의 세부 순위는 판별력이 없다.** 단, **멤버십 자체는 유효**: f120>0 계열 태그(지속매집 h60 +2.36 t6.24, 매집주 눌림 +2.32)는 유니버스 평균을 유의하게 이김. 결론 = "그 풀에 속하면 이긴다"는 성립, "그 풀 안에서 f20으로 더 세분 랭킹"은 불성립. → PR-2는 **정렬 세분화가 아니라 태그(멤버십) 기반 판정 컬럼**으로 — 이 측정이 근거.

**④ cons120(순매수일 비율)은 추가 가치 없음 — 도입 안 함.** 단독 IC ≈ 0(h120은 오히려 −0.0222 t−2.65), f120>0에 겹친 이중 게이트도 무의(최대 t1.72). `T_지속매집`(f120>0 & cons120≥0.5 & f20>0)은 유의(h60 +2.36)하나 진입권(+2.83)/정석(+3.51)보다 작아 **기존 태그 대비 우월하지 않음** — 런타임 미반영(파라미터 추가 없이 f120 부호 태그로 충분).

기존 ①② 수치는 기준일 2일 진행에 따른 데이터 리프레시만큼만 이동(정석 3.53→3.51 등), 순위·부호·유의성 보존. `_canonical_masks`/JSON 스키마 불변(--save scratchpad 검증 통과).

## PR-4a — 장기동시(외인+기관 20D·120D 4중) 측정 → 채택 (2026-07-05)

사용자 가설 "외인·기관이 20일 매집 중이면서 120일까지 둘 다 겹치는 종목이 베스트"를 측정. `i120` rolling 합 추가, 태그 2개 사전 등록(스윕 없음).

| h | **장기동시** (f20>0·i20>0·f120>0·i120>0) | 동시 | 정석 | 진입권 |
|---|---|---|---|---|
| 20 | **+1.81 (t4.74)** · 93종목/일 | +0.62 (2.76) | +1.63 (4.05) | +1.29 (5.00) |
| 60 | **+3.63 (t6.70)** · 91 | +1.03 (2.99) | +3.51 (5.16) | +2.83 (5.95) |
| 120 | **+7.53 (t5.67)** · 80 | +1.94 (3.59) | +7.49 (5.20) | +5.92 (5.80) |

- **✅ 채택 (매수 최상위 태그)**: 동시 대비 3배 크기·전 지평 유의, 정석과 동급 크기에 t 일관 우위, 임계값 없는 순수 부호 4중 게이트(파라미터 0), 종목수/일 80~93로 실용적. **사용자 직관이 데이터로 입증된 사례** (저점재매집 반박과 반대 방향). 배타 체인 갱신: **장기동시 > 정석 > 진입권 > 추세순항 > 동시**.
- 매도측 `동반순매도_장기`(+i120≤0): h60 −4.30(t−8.33) vs 기존 동반순매도 −3.36 — 유효하나 기존 경고가 이미 충분히 강해 **태그 신설 보류**(강도 세분화 한계효용 작음).
- 반영: `flow_verdict`(배타 체인)·`flow_metrics.long_both`·`_canonical_masks`·`_BACKTEST_DEFAULT` 4곳 동일 조건, JSON 재생성 완료.
- **파생 UI 교정 (두산로보틱스 사례)**: 매도 뷰가 f20 오름차순이라 장기매집주(f120 +38.7%)의 단기 이탈이 매도후보 2위로 노출 — 백테스트상 이는 '매집주 눌림'(+2.3% 강세)이라 모순. → 매도 뷰 **"장기매집 제외" 필터 기본 ON**(f120>0 숨김, 동반순매도의 약세 edge는 f120≤0 조건부라는 측정과 정합).

## PR-5 — 강세 후보 풀 좁히기 게이트 측정 (2026-07-05)

강세 후보 칩(강세 태그 ∧ 경고 없음)이 256종목으로 과대 — "지수보다 넓은 풀" 문제. 좁히기 게이트 3개 사전 등록 측정 (스윕 없음):

| 게이트 | h20 | h60 | h120 | 종목/일 | 판정 |
|---|---|---|---|---|---|
| **핵심A** (장기동시 ∧ 진입권) | +2.23 (3.92) | **+5.28 (5.34)** | **+12.63 (6.45)** | **28~32** | ✅ **채택** |
| 핵심B (장기동시 ∧ ret20>0) | +2.27 (5.06) | +4.41 (6.36) | +8.59 (5.85) | 66~75 | ❌ 좁히기 실패 (여전히 넓음) |
| 강세흡수 (장기동시∥정석 ∧ absorb5≥10%) | +0.81 (2.03) | +2.74 (3.97) | +6.49 (4.47) | 42~45 | ❌ **edge 역효과** |
| (비교) 장기동시 | +1.81 (4.74) | +3.63 (6.70) | +7.53 (5.67) | 80~93 | — |

- **핵심A 채택**: 풀 92→31종목/일(3배 축소)이면서 edge가 h60 +45%·h120 +68% 상승. 유일하게 "좁히면서 좋아지는" 게이트. 단 h5 t1.55 — 단기 트레이딩용 아님(중기 보유 전제). → 화면 **"핵심 후보" 칩** = 장기동시 ∧ entry_ok ∧ 경고 없음.
- **핵심B**: 예측력 자체는 최강(t)이나 66~75종목/일이라 축소 목적 탈락.
- **🔴 강세흡수 반직관**: 흡수율(진성 매집) 게이트가 강세 맥락에선 오히려 edge를 깎음(h60 2.74 < 3.63). absorb≥10% 상수는 반려된 저점재매집 출신 — 강세 풀 좁히기에 쓰지 말 것.
- **UI 부수**: 태그·판정 열에서 |t|<2 패턴 뱃지 동적 숨김(edges 기준, 하드코딩 없음 — '하락추세 매집' 노출 제거, 검증 통과 시 자동 재등장).

## PR-6 — 매도 타이밍 백테스트 (2026-07-05)

"좋은 조합에 들어갔으면 언제 파나" — 별도 스크립트 `backend/scripts/flow_exit_backtest.py` (flow_tag_backtest의 _fetch/_build_panel import 재사용, `PYTHONPATH=$PWD python3 scripts/flow_exit_backtest.py --years 2`). 진입 태그 4개(핵심A·장기동시·정석근사·매집주 눌림) × ① 보유기간 곡선(h5~180 + 구간 한계) ② 청산 규칙 5개(고정60/고정120/신호소멸/경고발생/먼저, onset 에피소드 단위) ③ 경고 태그 잔여수익.

**결론:**
1. **알파는 120거래일까지 계속 쌓인다** — 구간 한계 초과수익: 핵심A 0→60 +5.28 / 60→120 **+6.45**(후반이 더 큼) / 120→180 +4.38(t1.9 경계). 장기동시·매집주눌림은 120에서 소진(120→180 무의), 핵심A·정석은 180까지 여지.
2. **고정 ~120일 보유가 전 태그에서 총 초과수익 최대** (핵심A E2 +10.45 vs 경고매도 E4 +1.24 vs 신호소멸 E3 +0.54).
3. **신호소멸 매도는 부적합** — 부호 게이트 태그는 며칠 만에 off 토글(핵심A 평균 3.2일 보유) → 데이트레이드화, 총 edge 못 챙김. %/일은 최고(0.17)지만 절대액 미미.
4. **경고 발생 = 손절 아니라 로테이션 신호** — 장기동시 보유 중 경고(분배/동반순매도/단기반등) 발생 시 이후 60일 초과 +3.81→+0.70(차이 t−11.3). **음수 전환은 아님** → 대체 후보 있으면 교체, 없으면 계속 보유가 여전히 우월.
5. **매집주 눌림은 조기청산 절대 금지** (E5 ≈ 0 — 조기청산이 edge 완전 파괴).

**방법론 주의**: ⚠️ 유니버스 벤치를 일별 산술평균 누적으로 만들면 소형주 노이즈로 +7%→+14.5% 부풀어 부호 역전(Blume-Stambaugh) — **로그수익 평균 누적(기하)으로 교정** 필수. 에피소드 중첩으로 ②③ t 팽창(보수 해석). 단일 상승장 레짐(2024-07~2026-07) — 절대치는 레짐 의존, 순위·부호는 상대적 견고.

## PR-A — 검증 근거 리포트 (JSON 확장 + 화면 열람, 2026-07-06)

지금까지 CLI stdout·이 문서에만 있던 검증 곡선/Rank IC를 주기 갱신 JSON에 담아 수급 화면에서 열람.

- **JSON 확장** (`save_results`): 레거시 키(`patterns.{name}.{h60_excess_pct,t,direction,n_dates}` — flow_ai._load_edges 소비, 형식·값 불변)에 더해 `patterns.{name}.curve = {"<h>":{excess_pct,t,n_dates,avg_stocks}}`(CURVE_H=5·10·20·40·60·90·120·180, h60은 레거시와 동일 값 재사용), 최상위 `rank_ic`(HORIZONS), 메타(`lookback_years`·`period{start,end}`·`rebalance_days`·`universe_criteria`·`curve_horizons`·`method`) 추가. stdout ①~④ 섹션·HORIZONS 불변.
- **API**: `GET /api/flow/backtest-report` — JSON 원본 + `available` 반환(부재/파싱실패 시 `available:false`, 404 아님). 경로는 flow_ai `_BACKTEST_PATH` 재사용. 구스키마(curve 없음)도 그대로 반환 → 프론트가 필드 부재 처리.
- **화면** (`components/flow/flow-backtest-report.tsx`, "검증 근거" 토글): 헤더(기준일·룩백·유니버스·method) + Rank IC 미니테이블 + **보유기간 곡선(인라인 SVG, 매수 계열 기본 + 경고 계열 토글, 호버 값카드)** + 태그×h 매트릭스(`excess%(t)`, |t|≥2만 방향색). 열람 전용 — 재계산·파라미터 입력 없음(공식 1벌).

## PR-B — 종목별 태그 에피소드 히스토리 (2026-07-06)

패턴 평균 검증(PR-A)에 이어, 상세 차트에서 **"이 종목이 과거에 이 태그가 붙었을 때 실제로 어땠나"**를 열람. onset 마커(주가차트) + 에피소드 테이블(진입일·지속일·D+1 진입가·이후 20/60/120거래일 초과수익 vs 유니버스 평균). 사용자 파라미터 없음.

- **핵심 원칙 — 판정 공식 1벌 재사용:** 태그 판정을 새로 쓰지 않는다. 종목의 과거 각 거래일 D를 runtime 랭킹 SQL과 **같은 의미의 SQL-row로 재구성**해 `flow_metrics._row_to_metrics`를 **그대로** 통과시킨 뒤, 그 dict를 `flow_verdict.applicable_patterns`에 **그대로** 넘겨 일별 패턴 멤버십을 만든다(분모=as-of 유통비율×시총, bp 임계 15/−10, 부호 게이트 전부 정본에서). → 화면 태그(/ranking)와 히스토리가 바이트 일치. **정합성 게이트: 오늘 랭킹에서 태그 붙은 종목은 에피소드 타임라인 마지막 날(as_of)에도 같은 태그** (삼성전자·장기동시/동반순매도 종목으로 실측 확인).
- **universe_index (벤치마크)** — `save_results` JSON에 `universe_index:{dates,values}` 추가. 일별 유니버스(`adv20≥ADV_MIN & mcap≥MCAP_MIN`)의 **로그수익 평균 기하 누적**(시작 1.0, adj_open→adj_open). flow_exit_backtest.py의 uidx와 동일 방법론 — **산술평균 누적은 Blume-Stambaugh 편향으로 벤치가 부풀어 초과수익 부호가 뒤집힘**, 로그수익 평균 누적(경로독립)으로 교정. 2년 ≈ 490 포인트. 레거시/PR-A 키·stdout 불변. `main.py` startup stale 게이트에 `universe_index` 부재 조건 추가(PR-A rank_ic와 동일 방식 — 배포 시 자동 재생성).
- **성과 (look-ahead 차단):** 진입 = onset **D+1 시가**. h∈{20,60,120} 거래일 후 시가로 청산, 초과수익 = 종목수익 − 유니버스 인덱스 수익(entry_uidx→exit_uidx). 데이터 부족 지평은 null + 마지막 가용일 partial(진행중). onset = 전일 미보유→당일 보유, 에피소드 = onset~소멸 직전(지속일 기록). 부호 게이트 태그는 며칠 만에 토글돼(PR-6 평균 3.2일) 에피소드 多 — 병합·필터 없이 그대로 반환하되 stats(count·avg_excess_h60·win_rate_h60)로 요약.
- **서비스/API** — `backend/services/flow_episodes.py`(정본, data_version 캐시) + `GET /api/flow/episodes/{code}`(stock_code 정규화, 데이터 없으면 404). 벤치 부재(JSON 없음/구스키마)면 절대수익만 + `benchmark_available:false`. ⚠️ 조회는 파티션 락 누적(max_locks) 회피 위해 **테이블별 별도 세션**.
- **화면** — `flow-detail.tsx` 확장: 주가차트(①) onset 마커(강세 ▲초록 belowBar / 약세 ▼빨강 aboveBar, lightweight-charts v4 `series.setMarkers`, 차트 범위 내 날짜만) + 하단 **에피소드 섹션**(`flow-episodes.tsx`·`flow-episodes-utils.ts`): 패턴 칩(에피소드 有만, 개수 병기, 기본=배타 체인 최상위 매수) + 요약 줄 + 테이블(진입일·지속·진입가·+20/60/120D 초과%·진행중 뱃지·partial) + 각주. 열람 전용, 프론트 재계산 없음.
